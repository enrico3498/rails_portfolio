<% @posts.each do |post| %>
  <div class="well">
    <h2><%= post.title %></h2>
    <p><%= post.body %></p>
    <%= link_to "Mehr lesen", post_path(post), :class => "btn btn-default" %>
  </div>
<% end %>
